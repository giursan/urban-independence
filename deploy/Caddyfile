:8080 {
	encode zstd gzip

	@api path /health /chat /conversations* /diagnostics* /reports* /shares* /security-questions* /profile/phone /voice* /telegram*

	reverse_proxy @api api:8000
	reverse_proxy web:3000
}
