# --- Core (existing) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

ALLOWED_ORIGINS=http://localhost:3000

# --- Live HK tools (optional per tool; only required when the tool is invoked) ---
# Firecrawl powers web_search / web_scrape (general-purpose web fallback).
FIRECRAWL_API_KEY=

# Google Calendar tool. CALENDAR_ID is a default; the tool can take a per-call override.
GOOGLE_CALENDAR_API_KEY=
GOOGLE_CALENDAR_ID=
